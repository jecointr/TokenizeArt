{
  "name": "tokenizeart",
  "version": "1.0.0",
  "description": "42 TokenizeArt — ERC-721 NFT on Ethereum Sepolia Testnet",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run ../deployment/deploy.js --network sepolia",
    "mint": "hardhat run ../mint/mint.js --network sepolia",
    "owner": "hardhat run ../mint/ownerOf.js --network sepolia",
    "deploy:onchain": "hardhat run ../bonus/deploy-onchain.js --network sepolia",
    "mint:onchain": "hardhat run ../bonus/mint-onchain.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.4.5"
  }
}
